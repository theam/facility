{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", "build/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "dev": {
      "cache": false,
      "env": ["DATABASE_URL"],
      "persistent": true
    },
    "dev:worker": {
      "cache": false,
      "env": ["DATABASE_URL"],
      "persistent": true
    }
  }
}
