{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", "build/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "env": ["DATABASE_URL"],
      "persistent": true
    },
    "dev:worker": {
      "cache": false,
      "env": ["DATABASE_URL"],
      "persistent": true
    }
  }
}
