node_modules/
*.log
.DS_Store
.tmp/
.env
.env.*
!.env.example
.claude/worktrees/
.claude/launch.json
.turbo/

# Next.js build output
.next/
.docusaurus/
build/
dist/

# TypeScript incremental build cache
*.tsbuildinfo

# Terraform local provider cache + state (never commit — the provider binary is
# hundreds of MB; state can hold secrets). The .terraform.lock.hcl IS committed.
**/.terraform/
*.tfstate
*.tfstate.*
crash.log

# GitHub App private keys
*.pem
